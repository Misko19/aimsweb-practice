DATABASE_PATH=data/brightpath.db
BETTER_AUTH_URL=http://localhost:3000
BETTER_AUTH_SECRET=
# Offline generation only; never expose this as a NEXT_PUBLIC_ variable.
GEMINI_API_KEY=
# Comma-separated bare hostnames or IPs; no schemes, ports, paths, or wildcards.
ALLOWED_DEV_ORIGINS=127.0.0.1,localhost
